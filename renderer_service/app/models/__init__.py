"""
Domain models — the FloorPlanIR and everything it's built from.

See app/models/floorplan.py for the actual definitions. Kept as a
package (not a single top-level module) so future domain models (e.g. a
V2 3D scene graph) have an obvious place to live without reorganizing
what's already here.
"""
