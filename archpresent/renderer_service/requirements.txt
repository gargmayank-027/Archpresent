# Compatible-range pins, not exact — exact pins to a specific pydantic-core
# build broke on Python 3.14 (no prebuilt wheel for that pin, and the Rust
# toolchain it fell back to didn't support 3.14 either). Ranges let pip
# resolve to whatever current release actually has a wheel for the
# interpreter running the install, while still keeping upgrades bounded.
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.35
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-multipart>=0.0.9,<0.1

# PDF/image floor plan engine (app/services/pdf_vector_extract.py) — the
# only new runtime dependency this adds. The DXF engine remains stdlib-only.
pymupdf>=1.24,<2
